# Frontend

React + TypeScript + Vite client for Vamos Subscription Tracker.

## Run locally

```powershell
cd client
npm install
npm run dev
```

Default API URL: `http://127.0.0.1:8000`.

To override it, create `.env`:

```text
VITE_API_URL=http://127.0.0.1:8000
```

